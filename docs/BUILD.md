# Build

The package script bundles the extension as an VSIX file.
```sh
bun run package
```

# Install

To install the extension in VS Code, use the "Extensions: Install from VSIX..." command or `code --install-extension PATH_TO_VSIX`.
